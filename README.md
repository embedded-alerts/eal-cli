# eal-cli

Rust Embedded Alerts CLI integrated through flags-2-env for safe rule and operations workflows.

Initialized through `DEN-1949` as a testable `cli` foundation. Product behavior continues through focused pull requests.

```bash
python3 scripts/verify_repo.py
```
